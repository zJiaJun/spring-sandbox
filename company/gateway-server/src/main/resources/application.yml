server:
  port: 8080
spring:
  application:
    name: gateway-server
  cloud:
    gateway:
      enabled: true
      routes:
        - id: demo
          uri: http://baidu.com
          predicates:
            - Method=GET
    nacos:
      discovery:
        server-addr: 10.164.120.202:8848


